let min = function(x,y) { // Defines min to hold function value that takes two arguments
    if (x < y) { // If x is less than y
        return x; // Return the minimum, x
    } else { // If x is not less than y 
        return y; // Return the minimum, y
    }
    }

